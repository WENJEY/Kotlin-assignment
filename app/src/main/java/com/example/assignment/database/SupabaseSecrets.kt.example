package com.example.assignment.database

// Example template. Copy this file to `SupabaseSecrets.kt` (same directory) and
// fill in your project's Supabase URL and publishable/anon key.
//
//   cp SupabaseSecrets.kt.example SupabaseSecrets.kt
//
// `SupabaseSecrets.kt` is gitignored so real credentials are never committed.
// In Cloud Agent environments, `.cursor/install.sh` generates this file
// automatically from the SUPABASE_URL / SUPABASE_ANON_KEY environment secrets.
object SupabaseSecrets {
    const val url: String = "https://your-project-ref.supabase.co"
    const val apiKey: String = "your-publishable-or-anon-key"
}
