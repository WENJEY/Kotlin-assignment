<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Assignment" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="Theme.Assignment.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/logo_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/logo_round</item>
        <item name="windowSplashScreenIconBackgroundColor">#FFFFFF</item>
        <item name="postSplashScreenTheme">@style/Theme.Assignment</item>
    </style>

</resources>
